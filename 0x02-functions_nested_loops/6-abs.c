#include "main.h"

/**
 * _abs - Gets the absolute value of an integer.
 *
 * @i: The number you want to get its absolute.
 *
 * Return: Absolute value of i.
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	return (i);
}
